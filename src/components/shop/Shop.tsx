import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const Shop: FunctionComponent<Props> = (props) => {

  return (<div>shop</div>);
};

export default Shop;
