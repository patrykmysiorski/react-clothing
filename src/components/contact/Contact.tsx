import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const Contact: FunctionComponent<Props> = (props) => {
  return <div>Place for contact section</div>;
};

export default Contact;
