import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const DeliveryType: FunctionComponent<Props> = (props) => {
  return <div>Delivery type</div>;
};

export default DeliveryType;
