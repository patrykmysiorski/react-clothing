import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const Discount: FunctionComponent<Props> = (props) => {
  return <div>Do you have a rabacik?</div>;
};

export default Discount;
