import React, { FunctionComponent } from "react";

interface OwnProps {}

type Props = OwnProps;

const TotalPrice: FunctionComponent<Props> = (props) => {
  return <>You pay miliard</>;
};

export default TotalPrice;
