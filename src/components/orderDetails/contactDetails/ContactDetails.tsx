import { FunctionComponent } from "react";
import { Address } from "components/address-form/AddressForm";

interface OwnProps {
  address: Address | undefined;
}

const ContactDetails: FunctionComponent<OwnProps> = ({address}) => {
    return (
    <>
    </>
  );
};

export default ContactDetails;
